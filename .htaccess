# Rewrite engine
RewriteEngine On
# condition with escaping special chars
RewriteCond $1 !^(index\.js|robots\.txt|favicon\.ico)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ ./index.js/$1 [L,QSA]
